<div class="container">
    <div class="content">
        {{ exposed }}
        {% for row in rows %}
            {{ row }}
        {% endfor %}
        {% if pager %}
            {{ pager }}
        {% endif %}
    </div>
</div>